name: Release Please

# Consumers pin @v1 and expect it to follow patches/minors - move-floating-tags
# handles that.
on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    uses: drumandbytes/reusable-actions/.github/workflows/release-please.yml@v1
    with:
      move-floating-tags: true
    secrets: inherit
